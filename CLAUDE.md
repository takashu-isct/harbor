# CREW Harbor

学生団体の運営を1箇所にまとめるWebサービス。サービス名は「CREW Harbor」。
東京科学大学の学生団体 CREW が開発・運営する。
公開予定URL: harbor.isct-crew.jp

## 技術構成
- Next.js (App Router) + TypeScript
- ホスティング: Vercel
- データ保存: Googleスプレッドシート(サービスアカウント経由)
- 認証: Googleログインのみ(Auth.js)

## 絶対に守るルール
- 独自のパスワード認証を実装しない。Googleログインのみ
- APIキー・秘密鍵をコードに書かない。必ず環境変数
- .env.local は必ず .gitignore に入れる
- スプレッドシートの読み書きは lib/sheet.ts に集約する
- 色は直接指定せず、CSS変数(トークン)経由で使う
- ユーザーが入力した自由記述(氏名・役職・団体名・リンクの表示名・会計の内容など)を
  スプレッドシートに書き込む際は、lib/sheet.ts の sanitizeCell() を必ず通す
  (先頭が = + - @ だとGoogleスプレッドシート側で数式として実行されてしまうため)
- 外部リンクのURLはhttp/https以外のスキーム(javascript:など)を保存・保存前チェックなしで
  href に使わない
- Server Action(フォームのaction)は、ページ側の権限チェックに加えて、action関数の内部でも
  必ずセッション・権限を再確認する(URLを直接叩かれても安全なように)

## サービスの構造
- CREW Harborはワークスペース。中身は2種類に分かれる
  1. Harbor純正機能(会計・文書管理・ドライブ・イベント) … CREWが提供・責任を持つ
  2. 外部サービスへのリンク … 団体や個人が自由に登録するブックマーク
- 画面上でこの2つは見出しを分けて表示する
- 純正機能に個別のブランド名は付けない

## データ構造(スプレッドシート12枚)
1. 人 … 個人アドレス / 氏名 / 内部ID / 登録日 / 非表示ツール
   - 非表示ツールは、個人画面で本人が非表示にしたツールIDをカンマ区切りで入れる列
2. 団体 … 団体ID / 団体名 / 状態 / 設立日 / 非表示ツール
   - 非表示ツールは、団体画面で管理者が非表示にしたツールIDをカンマ区切りで入れる列
3. イベント … イベントID / イベント名 / 開催日 / 状態
4. 団体所属 … 個人アドレス / 団体ID / (未使用) / ロール(カンマ区切り、複数可) / 有効期限
   - 役職とロールは同じもの。「ロール」列に、その人がその団体で持つロール名をカンマ区切りで入れる
   - 1人が複数のロールを同時に持てる(Discordのロールと同じ考え方)
   - 割り当てられたロールのうち1つでも「管理者権限」を持つロールがあれば、その人はその団体の管理者として扱う
5. イベント参加団体 … イベントID / 団体ID / 立場
6. イベント個人参加 … 個人アドレス / イベントID / 役割 / 権限
7. リンク … 所属先ID / 表示名 / URL / アイコンURL / 公開範囲
   - 所属先IDには団体ID・イベントID・個人アドレスのいずれも入りうる
   - 登録できるのは団体の管理者のみ(個人ブックマークは本人のみ)
   - 外部リンクには「別ログインが必要」と明示する
8. 会計 … 団体ID / 日付 / 内容 / 金額 / 種別(収入・支出) / 登録者 / 登録日時
   - 団体に所属する人なら誰でも記帳・閲覧できる(将来、権限を絞る可能性あり)
9. ロール … 団体ID / ロール名 / 管理者権限
   - 団体ごとに自由にロールを追加・編集(名前の変更、管理者権限の付け外し)できる(Discordのロールに近いイメージ)
   - 現時点では「管理者権限があるかどうか」の1軸のみ。将来、権限項目を増やす可能性あり
   - 団体所属シートの「ロール」列には、ここで定義したロール名をカンマ区切りで入れる
   - ロール名を変更すると、そのロールを持つ全員の団体所属シートの記載も自動で置き換わる
10. 申請 … 団体ID / 個人アドレス / 氏名 / 希望役職 / 状態(未処理・承認・却下) / 申請日時 / 希望団体名
    - 「人」シートに未登録のメールでログインした人が、個人画面から所属したい団体に送る申請
    - 団体管理者が承認すると、人への登録と団体所属への追加が行われる
    - 団体IDが空欄の行は「新しい団体を立ち上げたい」という設立申請。希望団体名列に希望の団体名が入る
      - 既存の団体がないため団体管理者による画面上の承認はできない。
        「Harbor管理」画面でCREW開発部が団体IDを決めて承認すると、団体・ロール・団体所属が自動で作成される
11. 文書 … 文書ID / 団体ID / タイトル / 種別(空欄=通常の文書、folder=空フォルダの目印) / 内容 / 作成者 / 作成日時 / カテゴリ
    - 議事録に限らず団体内の文書全般を置く「文書管理」機能。日付は持たない(開催日などは本文に書く)
    - 文書IDはランダムな文字列(UUID)を自動採番する
    - 本文はMarkdownで書く(react-markdown + remark-gfmで表示。HTMLをそのまま埋め込むdangerouslySetInnerHTMLは使わない)
      - 作成画面は左に入力欄・右にリアルタイムプレビューを並べた分割エディタ
    - カテゴリはesaのような「ロール名/サブフォルダ/…」というスラッシュ区切りの階層(フォルダ)。
      先頭セグメントは必ずロール名と一致させ、閲覧・書き込みをロールに紐づける(閉鎖型)
      - 自分の持つロールのフォルダしか見えない・書けない。管理者権限を持つロールがあれば全フォルダを閲覧できる
        (ただし書き込みは管理者でも自分の持つロールのフォルダのみ)
      - サブフォルダから下は自由入力(例: 会計担当/月次報告/2026-08)
    - 「文書を作成」だけでなく「フォルダーを作成」もでき、中身が0件の空フォルダも作れる
      (種別列にfolderと入った、タイトル・内容が空の目印行として文書シートに1行追加する。
      一覧・検索には出さず、フォルダの存在と件数バッジの計算にだけ使う)
    - 保存先の指定方法は画面によって使い分ける
      - フォルダの中を開いている状態での「文書を作成」「フォルダーを作成」は、
        必ず「今開いているフォルダの直下」に作られる。保存先を選ぶ画面は出さない(タイトル/名前だけ入力する)
      - 保存先を選んで作りたいときは、文書管理のトップ画面(最近の文書)にある「新規作成」から入る。
        ここでは作るもの(文書/フォルダー)を選んだうえで、ロール(メインフォルダ)とサブフォルダ/タイトルを指定できる
    - 文書の編集ができる(タイトル・本文のみ。保存先フォルダの変更や削除機能はまだない)
      - 編集画面はタイトル・本文の分割エディタのみで、作成画面と違い保存先を選ぶ項目はない
        (保存先は固定。文書ID・カテゴリ・作成者・作成日時は編集で変わらない)
      - 「編集」へのリンクは、文書の詳細画面と、フォルダを開いたときの一覧の両方に
        書き込み権限がある場合だけ表示する(閲覧はできても書き込み権限がない文書には出さない)。
        「文書を作成」「フォルダーを作成」ボタンも同様に、書き込み権限があるフォルダでしか表示しない
        (管理者は全フォルダを閲覧できるが、書き込みは自分の持つロールのフォルダのみのため)
      - 文書を作成すると、そのフォルダの一覧ではなく作成した文書自体の詳細画面に移動する
        (フォルダーを作成した場合は、そのフォルダの一覧に移動する)
      - 作成・編集フォームは、本文(Markdown)を保存せずに他の画面へ移動しようとすると
        確認ダイアログを出す(UnsavedChangesProviderをメンバー管理画面と共通で使っている)。
        編集画面には保存せず戻れる「キャンセル」リンクもある
    - 画面はesaを参考に、左にフォルダツリーの常設サイドバー、右に一覧・詳細・作成フォームを表示する構成
      (角を尖らせたHarborのデザイントーンは維持し、esaの丸み・配色までは真似しない)
      - トップページは「最近の文書」フィード(アクセスできる全フォルダを横断して新着順、こちらは横断なので日付順のまま)
      - フォルダの中を開いたときは、エクスプローラーのように直下(その階層)の中身だけを表示する
        (サブフォルダの中身までは展開しない。トップの「最近の文書」フィードとは違う挙動)
      - フォルダには件数バッジを表示する(空フォルダの目印行はカウントに含めない)
      - 各記事の更新時刻はesaと同じく「3分前」「6日前」のような相対表示にする
12. クラウド … 団体ID / 表示名 / URL / ロール(カンマ区切り、複数可・空欄可) / 権限(reader/writer)
    - 団体画面の「Drive」タイルの中身。Harbor自体はファイルを保存しない
      (Harborが使っているGoogleアカウントは個人の無料アカウントで保存容量が無いため)。
      団体が既に自分たちで管理しているGoogleドライブ等のフォルダへのリンクを、
      Harborの中に見やすくまとめて置くだけの機能(外部リンクの団体版に近いが、
      「Drive」という独立タイルの中に表示する点が外部リンクと異なる)
    - リンクの追加・削除は団体管理者のみ
    - リンクごとに「見せるロール」を指定できる(文書管理と同じくHarborのロールと連動)。
      ロールを指定しなければ団体に所属する全員に表示。指定すると、そのロールを
      持つ人と管理者だけに表示する(管理者は指定の有無にかかわらず常に全リンクを閲覧できる。
      書き込み側の「自分の持つロールのみ」という文書管理の制限は、クラウドには適用しない
      〈単なるリンクの登録で、団体管理者なら誰でもどのロール向けにも登録してよいため〉)
    - URLはhttp/https以外を保存しない(外部リンクと同じisSafeHttpUrlチェック)。
      GoogleドライブのフォルダーのURLを想定している(フォルダーIDをURLから抽出して使う)
    - 「権限」列(閲覧者=reader/編集者=writer)は、Harbor画面上の表示には使わないが、
      下記のGAS連携でGoogleドライブ側の実際の共有権限を設定する際に使う
    - Googleドライブの実際のアクセス権(誰がそのフォルダーを開けるか)は、Harbor自身は
      制御していない(HarborのUIでリンクを絞るのは「案内板」であり、本当の意味での
      アクセス制限にはならない)。実際の権限をHarborのロールと自動的に一致させたい場合は、
      gas/harbor-drive-sync.gs(リポジトリ同梱)を、CREW開発部が新しく用意する
      専用のGoogleスプレッドシート(権限管理の作業専用。空のシートでよく、
      必要なタブはスクリプトが初回実行時に自動で作る)に導入する。Harborの団体所属・
      人・クラウドシートを直接参照して「あるべき権限」と「今の実際の権限」を突き合わせ、
      差分だけをGoogleドライブ側に反映する(毎晩自動実行するトリガーを設定可能)。
      導入手順・注意点はそのファイル内のコメントを参照。この連携はHarbor本体
      (Next.js/Vercel)とは別に動く仕組みで、導入・認可はCREW開発部が
      Googleアカウント側で行う必要がある

## 権限の考え方
- 権限は「人」ではなく「所属(立場)」に付く
- 1人が複数の団体・イベントに別々の権限で所属しうる
- Googleログインが成功しただけではアクセスさせない。
  シート1に該当メールが存在し、有効な所属があるかを必ず確認する
- 招待制。ユーザーの自己登録で新規に人を作らせない
  - ただし「人」シートに未登録の状態でログインした場合、拒否はしない。
    個人画面で所属したい団体への「申請」、または新しい団体の「設立申請」を送れるようにする
  - 参加申請は団体管理者が承認したときだけ、人への登録・団体所属への追加を行う
  - 設立申請は既存の団体がなく管理者もいないため、開発部が確認のうえ手動で団体を作成する
- 団体ごとに複数の管理者を置ける。「管理者権限」はロール単位で持つ
  (誰か1人だけが管理者、という制約はない)
- ロール=役職。1人が複数のロールを同時に持てる。管理者権限を持つロールを1つでも持っていれば管理者
- 団体に最低1人は管理者が必要。最後の1人の管理者は降格・削除できない(画面上でブロックする)
  - メンバー個人の降格・削除だけでなく、ロール自体から管理者権限を外す操作も対象。
    その変更をすると団体の管理者が誰もいなくなる場合は、ロール管理画面側でもブロックする

## 名簿管理の運用
- スプレッドシートを直接編集してよいのはCREW開発部のみ
- 団体管理者は、自分が管理者権限を持つ団体の情報(所属メンバーなど)を編集できる専用画面を持つ
  - 独立したアイコンは作らない。団体画面の中の一機能として提供する(管理者以外には出さない)
  - 「メンバー管理」画面: メンバーの追加・削除、たまっている参加申請の承認・却下、
    メンバーへのロールの割り当て(複数選択可)を行う
    - メンバーの追加は常時表示せず、「+メンバーを追加」ボタンから開くポップアップで行う
    - 各メンバーのロール欄は「更新」ボタンで保存する方式。ボタンは基本グレーで、
      チェックを変えて保存前の状態になると色が変わり、未保存だとわかるようにする
    - 未保存の変更があるまま他の画面へ移動しようとしたら、確認ダイアログを出してブロックする
      (UnsavedChangesProviderで実装。documentリンクのクリックとbeforeunloadの両方を監視)
    - 名簿はCSVでダウンロードできる(/org/[id]/members/csvのRoute Handler。
      CSVインジェクション対策として、セル内容が=+-@で始まる場合は先頭に'を付けて無害化する)
  - 「ロール管理」画面: ロール(役職)自体の追加・編集(名前変更・管理者権限の付け外し)を行う
    (Discordのロール管理に近いイメージ)。メンバーへの割り当てはメンバー管理画面側で行う
    - ロール名は団体内で重複できない(追加・改名どちらも重複はブロックする)
    - 更新ボタンの未保存表示・移動時の確認ダイアログは、メンバー管理画面と同じ仕組みを使う
    - 「メンバーを追加」「参加申請の承認」でロールを選ぶ際、既定でチェックする役職は
      「メンバー」という名前のロールがあればそれ、無ければ管理者権限を持たない先頭のロールを使う
      (「メンバー」ロールが存在しない団体で、うっかり無権限のまま追加されるのを防ぐため)。
      参加申請の承認では、申請者の希望役職が実在するロール名と一致する場合はそちらを優先する
- 一般ユーザーからの変更依頼(退会・情報修正など)は所定のフォームで受け付け、
  開発部または該当団体の管理者が反映する。スプレッドシートへの自己アクセスはさせない
- 未登録の人が団体に加わりたい場合は「申請」を送り、団体管理者が画面上で承認・却下する

## Harbor管理(CREW開発部の作業)
- CREW開発部は、CREW自体の団体(団体ID: crew)の中に「CREW開発部」という名前のロールを作り、
  そのロールを持つ人だけが使える特別な管理画面
- 開発部ロールの付与は、CREW団体のメンバー管理・ロール管理画面(通常の団体管理者向け画面)から行う。
  新しい開発部メンバーが増えても、この指示書やコードを変更する必要はない
- 「CREW開発部」ロールを持つ人には、プロフィール画面・各団体画面に「Harbor管理」という
  特別なアイコン(🛠️)が表示される
- 「Harbor管理」画面でできること: 団体の設立申請の確認・承認(団体IDを決めて作成)・却下
  - 承認すると、団体の新規作成・デフォルトロールの作成・申請者を管理者として団体所属に追加、
    までが自動で行われる(スプレッドシートを直接開いて編集する必要はない)
- 将来、開発部が行う他の作業(名簿の例外対応など)もこの画面に追加していく想定

## 画面構成
- ログイン後、立場が1つならその画面を直接開く。
  2つ以上なら選択画面を出す
- 画面上部に「ステータス」として今どの立場(個人/団体)で見ているかを常時表示し、そこから個人・所属する
  各団体へワンクリックで切り替えられる(プルダウン式の立場切り替え)
- 個人画面(/me)は団体画面とほぼ対等な立ち位置の「ホーム」。正方形アイコンを並べる構成は団体画面と共通
  - プロフィール(氏名・所属団体・自分の申請一覧の詳細は /me/profile に分離)
  - 参加イベント(準備中)、イベントを企画する(準備中ページ)、就活支援(準備中、将来追加予定の個人向けツール枠)
  - 「人」シートに未登録の人が個人画面を開いた場合は、
    団体を選んで申請できるフォーム、または新しい団体の設立申請フォームを表示する(拒否して終わりにしない)
- 団体画面: 会計 / 文書管理 / Drive / その団体が参加するイベント / イベントを企画する(現在は準備中ページのみ)
  - Harbor純正機能・外部リンクは、スマホのホーム画面のような正方形アイコンを並べて表示する
  - 外部リンクは純正機能と見出しを分けて表示し、「別ログインが必要」と明示する
  - 外部リンクの追加は、グレーの四角に+を重ねたアイコン(Chromeのショートカット追加のようなイメージ)から行う。
    表示名・URL・アイコン画像URL(任意)を入力する。アイコン画像URLが未入力の場合はリンク先サイトの
    アイコンを自動取得して表示する(実ファイルのアップロードには対応しない)
- 管理者用のアイコンは作らない。各画面の中にタブとして出し分ける
- 所属が0件の人には「まだ所属がありません」の案内を出す
- 表示するツール(アイコン)は非表示にできる。個人画面は本人が、団体画面は団体管理者が選ぶ。
  設定画面は独立したアイコンにはせず、各セクション見出し(CREW Harbor / 外部リンク / 個人)の
  右上に小さな「⚙️ 表示設定」「⚙️ リンク管理」リンクとして置く。管理者用アイコン
  (メンバー管理・ロール管理・Harbor管理など)は権限で自動的に出し分けられるため、
  この非表示設定の対象には含めない
- 承認待ちの申請など、管理者が対応すべき項目は「タスク」という言葉を使って表示し、
  何をすべきか一目でわかるようにする(例: メンバー管理画面の参加申請、Harbor管理画面の設立申請)。
  未処理件数は、団体画面・個人画面のメンバー管理アイコン/Harbor管理アイコンに赤いバッジ数字でも表示する

## デザイン
- 基調: ダークネイビー #0F1B2D / 面 #1B2A41
- 文字: #E8EDF4 / 補助 #9AA8BC
- アクセント: オレンジ(団体カラー)。イベントページでは差し替え可能にする
- 警告 #E0B33C / エラー #E2574C / 成功 #3FA96A
- ダークモードを標準とする(初回訪問時のデフォルト)。ライトモードにも切り替えられる
  - 個人画面の「表示設定」(/me/settings)にあるテーマ切り替えで、Cookie(theme)に保存する
  - 色は globals.css の `:root` (ダーク)と `:root[data-theme="light"]` (ライト)の
    2箇所にトークンを定義しているだけ。画面側は必ずトークン経由の色クラス(bg-background /
    text-foreground など)を使えば、新しい画面を追加しても自動的に両テーマに対応する
  - ロゴ・ヒーロー画像などの画像素材はテーマに合わせた差し替えをしていない
    (ロゴは白背景の枠に載せているため、どちらのテーマでも自然に見える)
- 丸みを帯びた形は使わない。ボタン・カード・アイコン枠など、角は尖らせたフォーマルな見た目にする
  (Tailwindの rounded-* は使わず rounded-none で統一)

## 開発の進め方
- 機能を横に広げず、1本ずつ端まで通す
- 各ステップでコミットし、Vercelへのデプロイまで確認する

## 私(依頼者)について
- プログラミングの専門知識はありません
- 専門用語を避け、平易な言葉で説明してください

## 進め方の権限(2026-08-20更新)
- コミット・プッシュ・ファイル作成編集・Vercelへのデプロイ確認など、私のパソコンやGitHub/Vercel上で完結する作業は、都度の許可を待たずに進めてよい
- 以下は必ず先にまとめて確認する:
  - Googleアカウントでのログインや外部サービスの新規登録・設定変更など、依頼者本人にしかできない操作
  - 料金が発生する可能性がある操作
  - 元に戻しにくい操作(強制上書き、削除、公開リポジトリの可視性変更など)
  - 仕様に書かれていない大きな設計判断が必要な場合
- 上記に該当しない実装作業は、この指示書の方針に沿って自律的に進めてよい
